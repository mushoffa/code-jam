#include <stdio.h>
#include <stdlib.h>
#include <vector>
using namespace std;

const int MAX_SIZE = 10 ^ 4;
// char in[max];
// int in[MAX_SIZE];

void test_case() {
	// int m;
	// int n;

	// scanf("%d", in);
	vector<pair<int, int>> in(2);
	// scanf("%d %d", &m,&n);
	// printf("%d",in);

	// printf("%d %d", m, n);

	puts("");
}

int main() {
	int T;
	scanf("%d", &T);

	// tc means test case
	for(int tc = 1; tc <= T; tc++) {
		test_case();
	}
}